This directory contains sample programs for Ada. For a complete Ada reference, download [Learning Ada](https://learn.adacore.com/pdf_books/learning-ada.pdf).

Each directory contains a file called `build-and-run.sh`. This script will build and run the program. It will also delete build artifacts and the executable file.
